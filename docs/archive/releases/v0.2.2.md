# Loop Engineering v0.2.2

> 归档：此文件保留历史 Release 事实，不描述当前产品模型。

- Repository Release: `v0.2.2`
- Workflow Definition: `github-pr-review/v0.1.2`
- Definition status: Candidate
- Stable status: no Stable Supported Profile

Repository Release 与 Workflow Definition 分别版本化。`v0.2.2` 发布 `github-pr-review/v0.1.2` 纠正版；它不改写 Repository Release `v0.2.0` / `v0.2.1`、Workflow Definition `github-pr-review/v0.1.0` / `v0.1.1` 或旧 Consumer Evidence Bundle。`v0.1.1` 的真实 Consumer 验证结论为 FAIL。

## Included

- 与 `v0.1.0`、`v0.1.1` 并存、可整体复制并由 Consumer Project 自有的完整 `github-pr-review/v0.1.2` 文件集合。
- review job 冻结到 `[self-hosted, Linux, X64, codex]`，route 与 trusted publish jobs 继续独立运行。
- 维护版 `tutar/codex-action` 锁定到 commit `f33581290086e62dc34d420a7f1862477fc2b503`，上游基线锁定为 `openai/codex-action@86365089eb2b84e0a8fb0717b304f8bdcb13b20e`，并显式固定 `codex-version: 0.153.4`。
- Runner-backed Codex Authentication：不传 repository `OPENAI_API_KEY`，由 Action 使用 runner 默认 Codex home 中的既有登录状态；Workflow 不直接调用 runner CLI。
- 保留 write-access gate、same-repository PR、Read-only Effect Mode、结构化 Standards/Spec 双轴结果、可信 Check publisher、SHA freshness、幂等与 fail-closed 边界。

## Evidence boundary

Candidate Publication Gate 提供 local static and automated contract evidence（本地静态与自动化契约证据），包括维护版 Action 对无 key/API-key 两条 server-info 控制流的回归测试；它仍不证明真实 runner、登录状态、网络或 Consumer Project 兼容。公开 Consumer Project 必须以该冻结组合完整执行相关 Cases，并产生独立 Evidence Bundle；不得拼接 `v0.1.0` 或失败的 `v0.1.1` 结果。

forks、共享 runner、无 write 权限 actor、其他 runner labels、API-key 路径、直接 CLI 与动态 Provider 标记为 Not Yet Verified 或不属于该 Definition。Stable Promotion 与安全失败路径仍由后续 ticket 负责。
