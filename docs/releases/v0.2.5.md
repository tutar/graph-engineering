# v0.2.5：从当前任务布局冻结 PR Review Candidate

Repository Release（仓库发布版本）`v0.2.5` 固定到 commit `927bd96156f750546019581b653b7601db9c71c8`，交付 Workflow Definition（工作流定义）`github-pr-review/v0.1.5` Candidate。安装内容直接来自 `workflow-tasks/pr-review/files/.github/`，不恢复历史版本源码目录，也不合并 PR #46 的旧目录形态。

源码中的 Definition/Profile 保持 `github-pr-review/current` / `github-pr-review/codex/current`；它们是当前实现身份，不是不可变发布版本。精确发布内容由 commit、tag 与 [`delivery/definitions.json`](../../delivery/definitions.json) 的独立交付身份追溯，Consumer 复制后自行拥有普通文件，运行时不访问本仓库。

完整 Check history 分页、精确身份协调、重复/畸形历史 fail-closed、写入前 head freshness 重读与非敏感发布审计已经包含在当前实现中。固定 Action `tutar/codex-action@f33581290086e62dc34d420a7f1862477fc2b503`、Codex CLI `0.153.4`、runner 登录方式和独立可信 publisher 的权限边界不变。

Bundle 7 使用私有独立 Consumer、全新 PR 和全新冻结组合；历史 Bundles 1–6 均不继承、不拼接。私有运行、Check 和 artifact 链接要求对应仓库访问权限，公开清单不复制私有业务内容、凭据或敏感日志。Candidate Publication Gate、本地测试、真实功能 Case 与 Stable Promotion Gate 是独立结论；本 Release 不声明 Stable Supported Profile。

Development Task 保持独立入口与权限，本 Release 不对其迁移后当前布局追加新的能力或真实兼容声明。旧 tag、Release 内容和失败 Evidence 保持不变。
